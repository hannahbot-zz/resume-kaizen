<%= form_for(@submitter_resume) do |f| %>
  <% if @submitter_resume.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@submitter_resume.errors.count, "error") %> prohibited this submitter_resume from being saved:</h2>

      <ul>
      <% @submitter_resume.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
